# MoonCAS 项目申报书

## 基本信息

- 项目名称：MoonCAS——面向 MoonBit 的内容寻址存储组件
- 参赛者：Yingqingxue（GitHub ID）
- 联系方式：在报名问卷中填写本人手机号或邮箱
- GitHub：https://github.com/Yingqingxue/mooncas
- 项目方向：新生态项目建设（存储与开发工具基础组件）
- 是否为移植项目：否，原创项目

## 项目简介

我在整理构建产物和版本文件时发现，用文件名或业务主键保存对象，很难回答“这两份内容是否完全相同”“下载结果是否被改动”“旧版本里哪些块还能复用”这些问题。MoonBit 生态已有面向 S3、R2 等服务的对象存储接口，但对象名仍由调用方决定；构建缓存、包制品和同步工具如果都各写一套哈希、校验与回收逻辑，接口和错误处理很容易分散。MoonCAS 把“内容本身决定标识”做成独立组件：写入字节后计算 SHA-256，以摘要作为键；再次写入相同内容不会产生第二份副本，按预期摘要写入时会先校验，不一致就拒绝。

这个项目补的是对象存储之上的语义层，而不是再做一个云存储 SDK。上层工具只认识稳定摘要，底层以后可以换成本地目录或远端对象存储。当前版本已用纯 MoonBit 完成摘要、内存后端、校验写入、引用计数和分块对象，并以 13 次有效提交保留开发过程；7 个核心测试可在 wasm、wasm-gc、JavaScript 三个后端运行。第一版选择固定大小分块，是为了先把分块顺序、缺块报错和引用释放做成可复现行为，内容定义分块留到后续版本验证。

## 预期使用场景

1. **构建缓存。** 编译器或 CI 把源码、配置和编译结果的字节内容写入 MoonCAS，记录返回的摘要。相同产物即使来自不同目录也只保存一次；读取时重新校验摘要，可以发现缓存损坏，而不只是判断文件是否存在。
2. **包与制品登记。** 发布工具拿到声明的 SHA-256 后调用校验写入；上传内容与声明摘要不一致时立即返回错误。下载方按摘要取回并验证，可把“版本号指向什么内容”和“内容是否正确”分开处理。
3. **大文件版本同步。** 文件按固定大小切块并保存块摘要。新版本只需传输尚不存在的块；所有块按清单顺序取回后重组。版本被删除时解除相应引用，再由回收操作删除未被引用的数据，避免直接清理仍在使用的块。

## 核心功能范围

- 纯 MoonBit 的 SHA-256 摘要计算、十六进制表示与内容校验；摘要字段保持私有，避免构造无效值。
- `put/get/has/delete` 基础接口，以及 `put_verified/verify` 校验接口；删除仍被引用的对象时明确拒绝。
- `pin/unpin/collect` 引用计数与保守回收；计数下溢返回错误，不把误操作静默吞掉。
- 固定大小分块写入、按序重组、整组引用与解除引用；任一块缺失时返回具体错误。
- 可运行命令行示例，用于演示摘要计算、写入、读取和校验流程。

## 预期验收产物

- 一个公开、使用 Apache-2.0 许可证的 MoonBit 仓库，主分支不少于 10 次有实际内容的提交。
- 可被其他项目依赖的 MoonCAS 库、公开 API 文档和至少一个可直接运行的命令行示例。
- 覆盖重复写入、摘要不匹配、引用保护、回收、分块重组与缺块错误的核心测试；CI 执行 `moon check`、`moon build` 和三后端测试。
- README 写明安装、最小示例、API、错误语义和复现命令；完成审核后发布至 mooncakes.io。

## 工程边界与实现路径

第一阶段明确只做单进程内容寻址核心，不承诺文件系统原子落盘、多进程并发、分布式一致性、访问控制或加密；内存后端用于把语义和测试跑通，不把它描述成生产级持久化。分块清单目前由调用方持有，后续再设计清单自身的摘要与持久化格式。实现上没有采用“数据库保存业务 ID 到文件名映射”的路线，因为那仍需要额外处理内容去重和端到端校验；也没有把云厂商接口写进核心包，而是让摘要、引用和分块逻辑保持后端无关。

## 移植或参考说明

MoonCAS 为原创实现，不是从其他语言仓库移植，也未复制现有项目代码。SHA-256 使用公开标准算法，固定大小分块和引用计数属于通用工程方法。项目会在 README 中说明这些基础概念及后续引用来源；目前找到的 MoonBit 对象存储项目解决的是后端访问问题，与本项目的内容身份、完整性校验和块复用层次不同。
