# MoonCAS 项目计划书（方向 4：Content-Addressed Storage）

## 一、项目概述

MoonCAS 是一个以 MoonBit 实现的内容寻址存储内核：数据以 SHA-256 摘要作为唯一标识，相同内容只保存一次，并提供完整性校验、分块复用、引用管理与垃圾回收。它面向构建缓存、包管理器、制品系统、注册表和可复现流水线等上层基础设施。

## 二、真实需求与差异化

MoonBit 生态已有面向 S3/R2/GCS 的对象存储抽象，但“对象存储”与“内容寻址语义”并不相同。MoonCAS 不重复实现云厂商客户端，而是提供后端之上的稳定内核：内容身份、写入幂等性、按预期摘要导入、分块清单、可达性与回收规则。首版用内存后端建立跨平台、可测试的参考语义，后续接文件系统和远端对象存储。

## 三、已完成 MVP（可验收）

- 纯 MoonBit SHA-256，实现空串与 `abc` 标准向量测试；
- `put/get/has/delete` 与相同内容自动去重；
- `put_verified` 和 `verify`，拒绝摘要不匹配的数据；
- 引用计数 `pin/unpin` 与不可达 Blob 垃圾回收；
- 大对象固定大小分块、分块级去重与重组；
- 可运行 CLI 示例、Apache-2.0 许可证、README 和 GitHub Actions；
- Wasm、Wasm-GC、JavaScript 三后端 CI 设计。

## 四、技术方案

输入字节先计算 SHA-256，摘要的十六进制文本作为后端键。逻辑大对象拆为固定大小块，每块独立寻址，清单保存有序摘要及总长度。导入时重新计算摘要，读取时可显式校验。引用表记录存活根，GC 仅删除未被引用的对象。API 不绑定文件或网络，便于以后实现统一的 Memory、Filesystem 与 HTTP/S3 后端。

## 五、9 月 23—30 日计划

1. 9 月 23 日：完成并公开 MVP、测试、README、计划书及可追踪提交；
2. 9 月 24—25 日：增加序列化清单、批量操作和失败原子性测试；
3. 9 月 26—27 日：实现文件系统后端及临时文件原子替换；
4. 9 月 28 日：全后端测试、基准测试与故障注入；
5. 9 月 29 日：演示录像、Mooncakes 发布准备与文档校对；
6. 9 月 30 日：提交验收材料并冻结 `v0.1.0`。

## 六、验收指标

- 标准 SHA-256 向量全部通过；相同内容产生相同 ID；
- 损坏或摘要不匹配的数据不能进入可信存储；
- 任一分块缺失时重组明确失败，不返回部分结果；
- `moon test --deny-warn` 通过，README 中命令可复现；
- 公开仓库保留连续提交、Issue/里程碑和版本变更记录。

## 七、风险与控制

主要风险是与现有对象存储库定位重叠、文件后端原子性不足，以及“引用计数”无法自动处理未知外部引用。对应措施是明确只做 CAS 语义层；文件写入采用临时文件、校验后原子替换；GC 默认保守，由显式根集合驱动。首版不承诺分布式一致性、加密或无限规模索引，避免超出一周可验证范围。

## 八、长期路线

`v0.2` 增加文件系统与持久化清单，`v0.3` 增加流式写入和并发控制，`v0.4` 对接 HTTP/S3 类后端与标记-清扫 GC，之后探索 Merkle DAG、远程缓存协议和增量同步。

## 九、信息来源

- [2026 MoonBit 黑客松官方页面](https://moonbitlang.github.io/Hackathon2026/)：公开仓库、连续提交、README、测试与可复现演示要求；
- [MoonBit 官方包管理文档](https://docs.moonbitlang.com/en/stable/toolchain/moon/package-manage-tour.html)：项目结构、测试与发布约定；
- [Mooncakes `bit_objstore`](https://mooncakes.io/docs/mizchi/bit_objstore)：用于识别现有云对象存储抽象并确定差异化边界；
- 用户提供的《推荐(1).txt》：方向 4 的初始选题、MVP 和长期路线建议。
