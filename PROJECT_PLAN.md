# MoonCAS 项目申报书

## 基本信息

- **项目名称：** MoonCAS：MoonBit 内容寻址存储内核
- **参赛者：** Yingqingxue（GitHub ID）
- **联系方式：** 在报名问卷中填写本人手机号或邮箱
- **项目方向：** 新生态项目建设 / 通用存储基础库
- **GitHub：** https://github.com/Yingqingxue/mooncas
- **项目性质：** 原创项目

## 项目简介与生态定位

MoonCAS 用 SHA-256 摘要标识字节内容，提供去重写入、校验导入、引用保护、垃圾回收和固定大小分块。MoonBit 已有 S3/R2/GCS 对象存储抽象，但对象名由调用者决定，不能直接提供“相同内容得到相同 ID”的 CAS 语义。构建缓存、包管理器和制品仓库都需要这一层。MoonCAS 不重复实现云存储客户端，而是提供可放在内存、文件系统或远端对象存储之上的稳定语义内核。

## 预期使用场景

1. **构建缓存：** 编译器按输出内容摘要写入制品；不同任务产生相同字节时只保存一份，并可在读取时复验摘要。
2. **包与制品管理：** 注册表按摘要保存包、二进制与元数据；上传端声明预期摘要，服务端拒绝内容不匹配的数据。
3. **大文件同步：** 文件拆成独立寻址的块；版本间相同块直接复用，缺块时明确失败，引用释放后再回收无用块。

## 核心功能范围

- 纯 MoonBit SHA-256、不可由外部构造的 `Digest` 类型；
- `put/get/has/delete/put_verified/verify` 与内容去重；
- `pin/unpin`、禁止删除活跃对象、保守垃圾回收；
- 分块清单、跨对象块复用、分块引用保护与重组；
- CLI 最小示例、Wasm/Wasm-GC/JavaScript 测试和 CI。

## 技术路线、边界与验收产物

输入先计算 SHA-256，十六进制摘要作为后端键；分块对象由有序摘要清单描述，清单持有块引用。首版交付内存参考实现、公开 API、README、示例、核心测试、CI、Apache-2.0 许可证及 Mooncakes 包。首版明确不做分布式一致性、加密和远端协议；后续增加持久化清单、文件系统原子写入、流式哈希与 S3/HTTP 后端。

## 原创与参考说明

本项目未移植或复制其他仓库代码；SHA-256 按公开标准实现。Mooncakes 中的对象存储项目解决厂商 API 抽象，本项目解决内容身份、完整性、分块可达性和回收语义，定位不同。
